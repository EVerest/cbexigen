{{ element_comment }}
struct {{ struct_name }} {
    union {
        {%- for type, name in elements %}
        struct {{ type }} {{ name }};
        {%- endfor %}
    };
    {%- for type, name in elements %}
    unsigned int {{ name }}_isUsed:1;
    {%- endfor %}
};
